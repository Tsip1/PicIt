<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@mipmap/logo"
    tools:context="com.example.kowler.pickit2.MainActivity">
    <android.support.v7.widget.Toolbar
        android:id="@+id/my_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:elevation="4dp"
        android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"/>

    <RelativeLayout
        android:id="@+id/relativeLayout"
        android:layout_width="650px"
        android:layout_height="650px"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="83dp">

        <Button
            android:id="@+id/buttonCamera"
            android:layout_width="100px"
            android:layout_height="100px"
            android:layout_above="@+id/buttonMap"
            android:layout_alignEnd="@+id/buttonMap"
            android:layout_marginBottom="137dp"
            android:background="@mipmap/camera1"
            android:backgroundTint="@mipmap/camera1" />    <!-- android:background="@android:drawable/ic_menu_camera" -->


        <Button
            android:id="@+id/buttonGallery"
            android:layout_width="100px"
            android:layout_height="100px"
            android:layout_alignBaseline="@+id/buttonCamera"
            android:layout_alignBottom="@+id/buttonCamera"
            android:layout_alignParentEnd="true"
            android:layout_marginEnd="15dp"
            android:background="@mipmap/gallery"
            android:backgroundTint="@mipmap/gallery" />    <!-- android:background="@android:drawable/ic_menu_gallery"-->

        <Button
            android:id="@+id/buttonMap"
            android:layout_width="100px"
            android:layout_height="100px"
            android:layout_alignParentBottom="true"
            android:layout_alignParentStart="true"
            android:layout_marginStart="11dp"
            android:background="@mipmap/map"
            android:backgroundTint="@mipmap/map" />   <!-- android:background="@android:drawable/ic_dialog_map" -->
    </RelativeLayout>


</RelativeLayout>
